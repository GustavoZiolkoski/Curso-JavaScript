var numero = 5; // Se transformar o 5 em string colocando as aspas simples ele não será exatamente igual, pois o de baixo ele sem aspas simples será number.

if (numero === 5 ) {
    console.log("É o número 5 do tipo string.")
}

if (numero !== 5) {
    console.log("Não é o número 5 do tipo number")
}